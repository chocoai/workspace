<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whty.ebp.api.dao.PadPwdDao">


	<select id="find" parameterType="map" resultType="java.util.HashMap">
			select * from TA_TOUCH_PWD_USER where 1=1
			<if test="userId != null and userId != ''">and user_id=#{userId} </if>
			<if test="userPlatformCode != null and userPlatformCode != ''">and user_platform_code=#{userPlatformCode} </if>
	</select>

</mapper>